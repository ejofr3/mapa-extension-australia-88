{
  "name": "mapa-extension-australia-88",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Where Working Holiday Maker specified work counts toward a second or third Australian visa — by postcode, by industry, across the year.",
  "scripts": {
    "vendor:worker": "node scripts/vendor-maplibre-worker.mjs",
    "predev": "npm run vendor:worker",
    "dev": "vite",
    "prebuild": "npm run vendor:worker",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@protomaps/basemaps": "^5.7.2",
    "maplibre-gl": "^6.1.0",
    "pmtiles": "^4.4.1"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^8.2.0"
  }
}
